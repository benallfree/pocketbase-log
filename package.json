{
  "name": "pocketbase-log",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "version": "0.0.1",
  "dependencies": {
    "pocketbase-stringify": "link:pocketbase-stringify"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "microbundle": "^0.15.1"
  },
  "scripts": {
    "build": "esbuild src/index.ts --format=cjs --sourcemap --bundle --outfile=dist/index.js",
    "watch": "bun run build --watch"
  }
}
