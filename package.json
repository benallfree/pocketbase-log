{
  "name": "pocketbase-log",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsup src/index.ts --dts --format cjs --out-dir dist",
    "watch": "bun run build --watch"
  },
  "files": [
    "dist"
  ],
  "version": "0.0.1",
  "dependencies": {
    "pocketbase-stringify": "link:pocketbase-stringify"
  },
  "devDependencies": {
    "pocketbase-jsvm": "link:pocketbase-jsvm",
    "tsup": "^8.2.4",
    "typescript": "^5.6.2"
  }
}
